import { defineCommand } from "citty";

export default defineCommand({
  meta: { name: "info", description: "Show system and hardware info" },
  run() {
    console.log("Not implemented yet");
  },
});
